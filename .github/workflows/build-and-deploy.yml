name: Build and Deploy Node.js Application

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches:
      - main
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm ci
      - run: npm test

      - name: Build and push Docker image
        run: |
          docker build . -t ghcr.io/${{ github.repository }}:latest
          docker push ghcr.io/${{ github.repository }}:latest

    #   - name: Docker login
    #     uses: docker/login-action@v2
    #     with:
    #       username: ${{ secrets.DOCKER_USERNAME }}
    #       password: ${{ secrets.DOCKER_PASSWORD }}

    #   - name: Build Docker image
    #     run: docker build . -t your-dockerhub-username/your-app-name:latest

    #   - name: Push Docker image
    #     run: docker push your-dockerhub-username/your-app-name:latest

    #   - name: Deploy with PM2 in Docker
    #     run: |
    #       docker run -d --name your-app-name -p 80:3000 your-dockerhub-username/your-app-name:latest pm2 start npm --name "app" -- start
